<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Softpig">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MovilesClase.RegistrarRestaurante"></activity>
        <activity android:name=".View.ForgetPasswordActivity" />
        <activity android:name=".View.InstallationActivity" />
        <activity android:name=".MovilesClase.ContenedorActivity" />
        <activity android:name=".View.MainActivity" />
        <activity android:name=".View.AboutActivity" />
        <activity android:name=".View.DictionaryActivity" />
        <activity android:name=".View.LoginActivity" android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>